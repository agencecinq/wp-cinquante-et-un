{#
 # Home
 #
 # @package WordPress
 # @subpackage WPCinquanteEtUn
 #}

{% extends 'index.html.twig' %}

{%- block content -%}
	<main id="content">
		<h1>{{- post.title -}}</h1>
		<p>{{- post.foo -}}</p>

		{%- for post in posts -%}
			<article>
				<h2>
					<a href="{{- post.link -}}">
						{{- post.title -}}
					</a>
				</h2>
			</article>
		{%- else -%}
			<p>{{- __( 'No posts found.', 'wp-cinquante-et-un' ) -}}</p>
		{%- endfor -%}
	</main>
{%- endblock -%}
