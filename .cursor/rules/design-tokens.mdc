---
description: Conventions pour les tokens de design Tailwind v4 dans theme.css (noms en anglais, sizes en rem, pas de valeurs en dur dans les classes utilitaires).
globs: src/stylesheets/theme.css,**/*.twig
alwaysApply: false
---

# Design tokens (`theme.css`)

## Declare in `@theme`, never in Twig utilities

- Colors, radii, typography scales, and background images belong in `src/stylesheets/theme.css` inside `@theme`.
- Do not use arbitrary Tailwind values in Twig or CSS utilities for design tokens: no `bg-[#102345]`, no `bg-[linear-gradient(...)]`, no `[background:...]`.
- Use the generated utilities instead (`bg-page`, `text-accent`, `rounded-25`, etc.).

## Token names in English

All custom property **suffixes** in `@theme` must be in **English**, kebab-case:

```css
/* GOOD */
--color-page: #08090a;
--color-accent: #b3fca4;
--radius-25: --spacing(6.25);

/* BAD */
--color-fond-page: #08090a;
--color-vert: #b3fca4;
--color-bleu-profond: #102345;
--background-image-gradient-bleu: linear-gradient(...);
```

- The Tailwind class mirrors the token suffix: `--color-accent` → `text-accent`, `bg-accent`, `border-accent`.
- Starter palette (CINQ Design System, dark): `page` / `elevated` / `card` / `card-hover` (backgrounds), `primary` / `muted` / `subtle` / `on-accent` (texts), `accent` / `accent-hover` / `accent-soft` / `border` / `border-subtle`. Pistachio accent is for CTAs, nodes, active links and focus only. `text-subtle` is not used on `bg-card` (contrast drops below AA).
- Legacy French token names may still exist in old client databases (ACF class values); **never add new French token names**.
- When initializing a new project, declare the client palette with English names from the start.

## No preset section backgrounds in the starter

The starter must not ship decorative section backgrounds (gradients, branded fills) in Twig templates. Background colors and images are chosen per project via tokens and block content, not baked into layout shells.

## Sizes in `rem` (no `px` in utilities)

All dimensional values in Tailwind classes and CSS must use **rem** or the **spacing scale** (`--spacing`), never **px** arbitrary values.

Applies to: `font-size`, `width`, `height`, `gap`, `padding`, `margin`, `inset`, etc.

- **Prefer spacing utilities** when the design value maps to the scale: `w-11.5` (46px), `gap-6.5` (26px), `py-3.5` (14px), not `w-[46px]` or `gap-[26px]`.
- **Tailwind arbitrary values in Twig**: `w-[2.875rem]`, `gap-[1.625rem]`, never `w-[46px]`, `gap-[26px]`, `p-[14px]`.
- **`@theme` tokens** (`--text-*`, `--width-*`, etc.): declare in rem, not px.
- **CSS** (`base/`, `utilities/`, legacy `components/`): dimensional properties in rem or `calc(var(--spacing) * n)`.

Conversion reference: `rem = px / 16` (e.g. `46px` → `2.875rem` or `w-11.5`, `26px` → `1.625rem` or `gap-6.5`).

**Exceptions:** `border-width` utilities (`border-2`), box-shadow offsets in `@theme` (`--shadow-brutal-sm`), and HTML `width`/`height` attributes on `<img>` (intrinsic dimensions).

If a value is reused, add a token in `@theme` rather than repeating rem arbitrary values in Twig.

## Letter-spacing in `em`

All `letter-spacing` values must be expressed in **em**, never in **px**.

- **`@theme` typography tokens** (`--text-*--letter-spacing`, or dedicated tokens): declare in em (`0.025em`, not `0.5px`).
- **Tailwind classes in Twig**: `tracking-[0.0227em]`, never `tracking-[0.5px]` or `tracking-wide` when a precise design value is required (utility presets are acceptable for generic typography classes).
- **CSS**: `letter-spacing` in em only.

Conversion reference: `em = px / font-size-px` on the same element (e.g. `0.5px` at `22px` / `1.375rem` → `0.0227em`).

If a value is reused, add a token in `@theme` rather than repeating em arbitrary values in Twig.

## Typography scale (CINQ Design System)

Tokens and composite classes in `theme.css` / `typographies.css` follow the Figma scale. Prefer the composite class in Twig (`.title-xl`, `.text-sm`, `.eyebrow`) over raw `text-*-mobile` / `text-*-desktop` utilities.

- **Titles** (weight 500): `.title-3xl`, `.title-xxl`, `.title-xl`, `.title-lg`, `.title-md`
- **Text** (weight 400): `.text-xl`, `.text-lg`, `.text-base`, `.text-sm`, `.text-xs`
- **Meta** (Tailwind `font-mono`, same on mobile and desktop): `.eyebrow` (caps), `.label`, `.cta` (weight 500), `.badge` (caps)

The starter uses Tailwind / browser font stacks. Do not add a webfont unless the project specifies one (then align `@font-face` or the Google Fonts example in `Enqueue::enqueue_styles()`, and `--default-font-family`).

## Checklist when adding a token

1. English kebab-case suffix in `@theme`.
2. Used via a utility class in Twig/CSS, not an arbitrary value.
3. Sizes in rem or spacing scale, letter-spacing in em (see above).
4. `pnpm build` passes.
